[project]
name = "dotfiles-e2e"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
dotfiles-e2e = "src.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
