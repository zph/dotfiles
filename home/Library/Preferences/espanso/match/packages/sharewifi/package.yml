# Automatically generated by espanso migration tool
# Original file: packages/sharewifi/package.yml

matches:
  - trigger: ":qrsharewifi"
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: sharewifi -q
  - trigger: ":vsharewifi"
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: sharewifi -v
  - trigger: ":sharewifi"
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: sharewifi