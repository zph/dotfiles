#!/usr/bin/env ruby

require 'nokogiri'
require 'open-uri'

url = "http://www.gifbin.com/random"

page = open(url).read
parsed = Nokogiri::HTML.parse(page)

link = parsed.xpath('//*[@id="gif"]').first.attributes["src"].value
exec "echo '#{link}' | pbcopy ;open #{link}"
